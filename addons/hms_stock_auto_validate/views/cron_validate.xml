<odoo>
    <record id="ir_cron_auto_validate_transfers" model="ir.cron">
        <field name="name">Auto Validate Ready Transfers</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="state">code</field>
        <field name="code">model.auto_validate_transfers()</field>
        <field name="interval_type">days</field>
        <field name="interval_number">1</field>
        <field name="numbercall">-1</field>
        <field name="active" eval="True"/>
        <field name="doall" eval="False"/>
        <field name="nextcall" eval="datetime.now().replace(hour=16, minute=30, second=0, microsecond=0)"/>
    </record>
</odoo>