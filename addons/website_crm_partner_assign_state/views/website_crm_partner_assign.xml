<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="index" name="Find Resellers" inherit_id="website_crm_partner_assign.index">
        <xpath expr="//div[@id='partner_left']" position="replace">
            <div class="col-lg-4 mb32" id="partner_left">

                <!-- ul id="reseller_grades" class="nav nav-pills flex-column mt16">
                    <li class="nav-header nav-item"><h3>Filter by Level</h3></li>
                    <t t-foreach="grades" t-as="grade">
                        <li class="nav-item">
                            <a t-attf-href="/partners#{ grade['grade_id'][0] and '/grade/%s' % grade['grade_id'][0] or '' }#{ current_country and '/country/%s' % slug(current_country) or '' }#{ current_state and '/state/%s' % slug(current_state) or '' }#{ '?' + (search_path or '') + '&amp;' + keep_query('country_all') }"
                               t-attf-class="nav-link#{grade['active'] and ' active' or ''}">
                                <span class="badge badge-pill float-right" t-esc="grade['grade_id_count'] or ''"/>
                                <t t-esc="grade['grade_id'][1]"/>
                            </a>
                        </li>
                    </t>
                </ul>

                <ul id="reseller_countries" class="nav nav-pills flex-column mt16">
                    <li class="nav-header nav-item"><h3>Filter by Country</h3></li>
                    <t t-foreach="countries" t-as="country">
                        <li t-if="country['country_id']" class="nav-item">
                            <a t-attf-href="/partners#{ current_grade and '/grade/%s' % slug(current_grade) or ''}#{country['country_id'][0] and '/country/%s' % country['country_id'][0] or '' }#{ current_state and '/state/%s' % slug(current_state) or '' }#{ '?' + (search_path or '') + (country['country_id'][0] == 0 and '&amp;country_all=True' or '')}"
                               t-attf-class="nav-link#{country['active'] and ' active' or ''}">
                                <span class="badge badge-pill float-right" t-esc="country['country_id_count'] or ''"/>
                                <t t-esc="country['country_id'][1]"/>
                            </a>
                        </li>
                    </t>
                </ul -->
                <ul id="reseller_states" class="nav nav-pills flex-column mt16">
                    <!-- li class="nav-header nav-item"><h3 class="text-white">Filter by Provinsi</h3></li -->
                    <t t-foreach="states" t-as="state">
                        <li t-if="state['state_id']" class="nav-item">
                            <a t-attf-href="/partners#{ current_grade and '/grade/%s' % slug(current_grade) or ''}#{ current_country and '/country/%s' % slug(current_country) or '' }#{state['state_id'][0] and '/state/%s' % state['state_id'][0] or '' }#{ '?' + (search_path or '') + '&amp;' + keep_query('country_all') }"
                               t-attf-class="nav-link#{state['active'] and ' active' or ''}">
                                <span class="badge badge-pill float-right" t-esc="state['state_id_count'] or ''"/>
                                <t t-esc="state['state_id'][1]"/>
                            </a>
                        </li>
                    </t>
                </ul>
            </div>
        </xpath>
    </template>
</odoo>